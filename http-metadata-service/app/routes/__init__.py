"""
Routes package.

Contains API endpoint definitions (transport layer).
Routes are responsible for HTTP concerns only — request parsing,
response formatting, and status codes. All business logic is
delegated to the services layer.
"""